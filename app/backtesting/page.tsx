'use client'
import{FormEvent,useEffect,useMemo,useState}from'react';import{supabase}from'@/lib/supabase';import type{Backtest}from'@/lib/types';import{pairs,timeframes,scoreFrom,grade}from'@/lib/utils';import StatCard from'@/components/StatCard';
export default function Backtesting(){const[data,setData]=useState<Backtest[]>([]),[show,setShow]=useState(false),[pair,setPair]=useState('All');async function load(){const{data}=await supabase.from('backtests').select('*').order('historical_trade_date',{ascending:false});setData((data||[])as Backtest[])}useEffect(()=>{load()},[]);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const{data:{user}}=await supabase.auth.getUser();if(!user)return;const score=scoreFrom(f,false);const num=(k:string)=>f.get(k)?Number(f.get(k)):null;const payload:any={user_id:user.id,date_tested:f.get('date_tested'),historical_trade_date:f.get('historical_trade_date'),pair:f.get('pair'),direction:f.get('direction'),higher_timeframe_trend:f.get('higher_timeframe_trend'),entry_timeframe:f.get('entry_timeframe'),zone_type:f.get('zone_type'),entry_price:num('entry_price'),stop_loss:num('stop_loss'),take_profit:num('take_profit'),planned_rr:num('planned_rr'),result:f.get('result'),r_multiple:num('r_multiple'),setup_score:score,trade_grade:grade(score),lesson_learned:f.get('lesson_learned')||null};['valid_zone','liquidity_marked','liquidity_swept','fvg_present','fvg_mitigated','candle_confirmation'].forEach(k=>payload[k]=f.get(k)==='on');const{error}=await supabase.from('backtests').insert(payload);if(error)alert(error.message);else{setShow(false);load()}}const rows=useMemo(()=>data.filter(x=>pair==='All'||x.pair===pair),[data,pair]);const wins=rows.filter(x=>x.result==='Win').length,totalR=rows.reduce((s,x)=>s+(x.r_multiple||0),0);return <><div className="top-row"><div><h1 className="page-title">Backtesting</h1><div className="muted">Historical setups kept separate from live trading.</div></div><button className="button" onClick={()=>setShow(!show)}>{show?'Close form':'Add Backtest'}</button></div><div className="grid stats compact"><StatCard label="Total backtests" value={rows.length}/><StatCard label="Win rate" value={`${rows.length?(wins/rows.length*100).toFixed(1):0}%`}/><StatCard label="Average R" value={(rows.length?totalR/rows.length:0).toFixed(2)}/><StatCard label="Total R" value={totalR.toFixed(2)}/></div>{show&&<form className="card form-card" onSubmit={submit}><div className="form-grid"><label className="field">Date tested<input name="date_tested" type="date" required/></label><label className="field">Historical trade date<input name="historical_trade_date" type="date" required/></label><label className="field">Pair<select name="pair">{pairs.map(x=><option key={x}>{x}</option>)}</select></label><label className="field">Direction<select name="direction"><option>Buy</option><option>Sell</option></select></label><label className="field">HTF trend<select name="higher_timeframe_trend"><option>Bullish</option><option>Bearish</option><option>Ranging</option></select></label><label className="field">Entry timeframe<select name="entry_timeframe">{timeframes.map(x=><option key={x}>{x}</option>)}</select></label><label className="field">Zone type<select name="zone_type"><option>Demand</option><option>Supply</option></select></label><label className="field">Entry<input name="entry_price" type="number" step="any"/></label><label className="field">Stop loss<input name="stop_loss" type="number" step="any"/></label><label className="field">Take profit<input name="take_profit" type="number" step="any"/></label><label className="field">Planned RR<input name="planned_rr" type="number" step=".01"/></label><label className="field">Result<select name="result"><option>Win</option><option>Loss</option><option>Breakeven</option></select></label><label className="field">R multiple<input name="r_multiple" type="number" step=".01"/></label><div className="field full"><div className="checks">{[['valid_zone','Valid zone'],['liquidity_marked','Liquidity marked'],['liquidity_swept','Liquidity swept'],['fvg_present','FVG present'],['fvg_mitigated','FVG mitigated'],['candle_confirmation','Candle confirmation']].map(([k,l])=><label className="check" key={k}><input name={k} type="checkbox"/>{l}</label>)}</div></div><label className="field full">Lesson learned<textarea name="lesson_learned"/></label></div><button className="button">Save Backtest</button></form>}<div className="filters"><select value={pair} onChange={e=>setPair(e.target.value)}><option>All</option>{pairs.map(x=><option key={x}>{x}</option>)}</select></div><div className="table-wrap"><table className="table"><thead><tr><th>Date tested</th><th>Historical date</th><th>Pair</th><th>Direction</th><th>Result</th><th>R</th><th>Score</th><th>Grade</th></tr></thead><tbody>{rows.map(x=><tr key={x.id}><td>{x.date_tested}</td><td>{x.historical_trade_date}</td><td>{x.pair}</td><td>{x.direction}</td><td>{x.result}</td><td>{x.r_multiple??'—'}</td><td>{x.setup_score}%</td><td>{x.trade_grade}</td></tr>)}{!rows.length&&<tr><td colSpan={8} className="empty">No backtests yet.</td></tr>}</tbody></table></div></>}
