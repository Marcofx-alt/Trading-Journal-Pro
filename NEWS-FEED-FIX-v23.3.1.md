# News Feed Fix v23.3.1

- Tries both Fair Economy CDN and primary host for Forex Factory weekly JSON.
- Adds browser-like request headers for runtimes that reject bare server requests.
- Treats zero selected impact filters as "show all" instead of hiding every event.
- Adds an Impact Select all / Clear control.
- Keeps Trading Economics as the preferred source when a key is configured.
